import { joinPaths } from './pathHelper';
export function parseStoragePath(rawFile, docPath, fieldPath, useFileName) {
    var fileNameBits = rawFile instanceof File ? rawFile.name.split('.') : [];
    var fileExtension = !(fileNameBits === null || fileNameBits === void 0 ? void 0 : fileNameBits.length) ? '' : '.' + fileNameBits.pop();
    return useFileName
        ? joinPaths(docPath, fieldPath, rawFile.name)
        : joinPaths(docPath, fieldPath + fileExtension);
}
//# sourceMappingURL=storage-parser.js.map