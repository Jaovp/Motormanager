import { parseStoragePath } from '../src/misc';
test('useFileNamesInStorage: false', function () {
    var mockFile = new File([], 'test.png');
    var docPath = 'resource/id';
    var fieldPath = 'picture';
    var path = parseStoragePath(mockFile, docPath, fieldPath, false);
    expect(path).toBe('resource/id/picture.png');
});
test('useFileNamesInStorage: true', function () {
    var mockFile = new File([], 'test.png');
    var docPath = 'resource/id';
    var fieldPath = 'picture';
    var path = parseStoragePath(mockFile, docPath, fieldPath, true);
    expect(path).toBe('resource/id/picture/test.png');
});
//# sourceMappingURL=storagePath.spec.js.map